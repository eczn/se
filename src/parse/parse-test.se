;; 定义 xy 
(define xy (cons 1 2))

(log (car xy))
;; => 1

(log (cdr xy))
;; => 2


; (define lst
;     (cons 1 (cons 2 (cons 3 4))))
; ;; 你最爱的链表

; (log lst)

; ; (log (car (cons 1 2)))

; ; (define (cons x y)
; ;     (lambda (m) (m x y)))

; ; (define (car z)
; ;     (z (lambda (p q) p)))

; ; (define (cdr z)
; ;     (z (lambda (p q) q)))

