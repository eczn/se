(define add_xyz 
    (lambda (x) 
        (lambda (y) 
            (lambda (z) (+ x y z)))))

(define add_xyc 
    (add_xyz 'c))

(define add_xbc 
    (add_xyc 'b))

;; cba
(add_xbc 'a)

;; abc 
(((add_xyz 'a) 'b) 'c)
